# Semantic Scholar 論文取得・可視化システム

このプロジェクトは、Semantic Scholar APIを使用して2025年に発表された論文を取得し、インタラクティブなWebページで可視化します。

## 機能

### 📊 データ取得（Python）
- ✅ 2025年発表の論文を検索（LLM / VLM）
- ✅ 検索結果の総数を自動取得
- ✅ レート制限を考慮した安全な取得（指数バックオフ）
- ✅ 全結果の自動ページネーション
- ✅ タイトル、アブストラクト、引用数などを含むJSONL形式での逐次保存

### 🌐 データ可視化（GitHub Pages）
- ✅ 2つのデータセット（LLM / VLM）をタブで切り替え
- ✅ 3つのランキング表示
  - 🏆 2025年引用数 Top 100
  - 🔥 過去30日間 Top 50
  - 📈 引用速度（引用数/日数）Top 100
- ✅ 展開可能なリスト（アブストラクト、著者などの詳細情報）
- ✅ レスポンシブデザイン（PC・モバイル対応）

## セットアップ

### 1. 依存関係のインストール

```bash
pip install -r requirements.txt
```

### 2. APIキーの設定（推奨）

Semantic Scholar APIキーを取得して環境変数に設定します。

**APIキーの取得方法:**
1. [Semantic Scholar API](https://www.semanticscholar.org/product/api) にアクセス
2. "Request an API Key" をクリック
3. フォームに入力してAPIキーを取得

**環境変数の設定:**

```bash
# Linux/macOS
export SEMANTIC_SCHOLAR_API_KEY="your_api_key_here"

# Windows (PowerShell)
$env:SEMANTIC_SCHOLAR_API_KEY="your_api_key_here"

# または .env ファイルに記載
echo "SEMANTIC_SCHOLAR_API_KEY=your_api_key_here" > .env
```

**注意:** APIキーなしでも動作しますが、レート制限が厳しくなります。

## 使用方法

### 基本的な実行

```bash
python fetch_papers.py
```

### 実行例

```
検索中: 'Large language model' (年: 2025)
取得フィールド: paperId, title, abstract, year, citationCount, publicationDate, authors, url, venue, publicationTypes
--------------------------------------------------------------------------------
ページ 1 を取得中...
総検索結果数: 1,234 件
--------------------------------------------------------------------------------
1000 件取得（累計: 1,000 件）
ページ 2 を取得中... 234 件取得（累計: 1,234 件）

すべてのページを取得しました。

1,234 件の論文を semantic_scholar_llm_2025_20251218_143022.jsonl に保存しました。

================================================================================
統計情報
================================================================================
総検索結果数: 1,234 件
取得件数: 1,234 件

引用数統計:
  - 最大: 156 回
  - 最小: 0 回
  - 平均: 12.3 回
```

## 出力形式

結果は `semantic_scholar_llm_2025_YYYYMMDD_HHMMSS.jsonl` というファイル名で保存されます。

### JSONLフォーマット

各行は1つの論文を表すJSON形式です：

```json
{
  "paperId": "abc123def456",
  "title": "Example Paper Title",
  "abstract": "This is an example abstract...",
  "year": 2025,
  "citationCount": 42,
  "publicationDate": "2025-03-15",
  "authors": [
    {"authorId": "123", "name": "Author Name"}
  ],
  "url": "https://www.semanticscholar.org/paper/...",
  "venue": "Conference Name",
  "publicationTypes": ["JournalArticle"]
}
```

### データの読み込み例

```python
import json

papers = []
with open("semantic_scholar_llm_2025_20251218_143022.jsonl", "r", encoding="utf-8") as f:
    for line in f:
        papers.append(json.loads(line))

print(f"読み込んだ論文数: {len(papers)}")
```

## カスタマイズ

### 検索条件の変更

[fetch_papers.py](fetch_papers.py) の `main()` 関数内のパラメータを編集してください：

```python
# 検索クエリの変更
query = "Large language model"  # ← ここを変更

# 年の変更
year = "2025"  # ← ここを変更（例: "2024-2025" で範囲指定も可能）

# 取得フィールドの変更
fields = [
    "paperId",
    "title",
    "abstract",
    # 必要に応じて追加/削除
]
```

### 利用可能なフィールド

- `paperId`: 論文ID
- `title`: タイトル
- `abstract`: アブストラクト
- `year`: 発表年
- `citationCount`: 引用数
- `publicationDate`: 発表日
- `authors`: 著者リスト
- `url`: Semantic Scholar URL
- `venue`: 発表媒体（学会、ジャーナル等）
- `publicationTypes`: 論文タイプ
- `fieldsOfStudy`: 研究分野
- `s2FieldsOfStudy`: S2研究分野
- `openAccessPdf`: オープンアクセスPDF情報

## レート制限について

- **APIキーあり**: 1リクエスト/秒（デフォルト）
- **APIキーなし**: より厳しい制限

スクリプトは自動的に以下を実施します：
- リクエスト間の適切な待機時間
- レート制限エラー時の指数バックオフ
- 最大5回の自動リトライ

## トラブルシューティング

### レート制限エラーが頻発する場合

APIキーを設定するか、`fetch_papers.py` 内の `rate_limit_delay` を増やしてください：

```python
self.rate_limit_delay = 2.0  # 1.0 から 2.0 に変更
```

### タイムアウトエラーが発生する場合

ネットワーク環境が不安定な場合は、タイムアウト時間を延長してください：

```python
response = self.session.get(self.BASE_URL, params=params, timeout=60)  # 30 から 60 に変更
```

## GitHub Pagesでの公開

取得したデータを可視化するWebサイトをGitHub Pagesで公開できます。

### 必要なファイル

- `index.html` - メインページ
- `semantic_scholar_llm_2025_YYYYMMDD_HHMMSS.jsonl` - LLMデータ
- `semantic_scholar_vlm_2025_YYYYMMDD_HHMMSS.jsonl` - VLMデータ
- `.nojekyll` - Jekyll処理をスキップ

### セットアップ手順

1. **リポジトリを作成**
   ```bash
   # GitHubで新しいリポジトリを作成後
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/username/repository.git
   git push -u origin main
   ```

2. **GitHub Pagesを有効化**
   - リポジトリの Settings → Pages に移動
   - Source: "Deploy from a branch"
   - Branch: "main" / "root" を選択
   - Save をクリック

3. **ファイル名の更新**

   `index.html` の以下の部分を、実際のファイル名に変更してください：
   ```javascript
   const files = {
       llm: 'semantic_scholar_llm_2025_20251219_022918.jsonl',  // ← 実際のファイル名
       vlm: 'semantic_scholar_vlm_2025_20251219_023659.jsonl'   // ← 実際のファイル名
   };
   ```

4. **アクセス**

   5-10分後、以下のURLでアクセスできます：
   ```
   https://username.github.io/repository/
   ```

### ローカルでのテスト

GitHub Pagesにデプロイする前に、ローカルでテストできます：

```bash
# Pythonの簡易サーバーを起動
python -m http.server 8000

# ブラウザで開く
open http://localhost:8000
```

**注意:** ファイルシステムから直接HTMLを開く（file://）とCORS制約でJSONLファイルの読み込みに失敗します。必ずHTTPサーバー経由でアクセスしてください。

## ライセンス

このスクリプトはMITライセンスの下で公開されています。

## 参考リンク

- [Semantic Scholar API Documentation](https://www.semanticscholar.org/product/api)
- [API Tutorial](https://www.semanticscholar.org/product/api/tutorial)
- [GitHub Pages Documentation](https://docs.github.com/pages)
